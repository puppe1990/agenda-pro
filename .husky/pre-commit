#!/usr/bin/env sh
./node_modules/.bin/lint-staged || exit 1

if git diff --cached --name-only | grep -Eq '^(pnpm-lock\.yaml|apps/.+/package\.json|packages/.+/package\.json)$'; then
  pnpm --filter @agenda-pro/app audit:deps || exit 1
fi

pnpm test
