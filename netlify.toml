[build]
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @agenda-pro/landing exec vite build"
  publish = "apps/landing/dist/client"

[build.environment]
  NODE_VERSION = "22"
  VITE_APP_URL = "https://app.gestaobem.com"
  VITE_WHATSAPP_PHONE = "5521987962324"

[functions]
  directory = "apps/landing/.netlify/v1/functions"
